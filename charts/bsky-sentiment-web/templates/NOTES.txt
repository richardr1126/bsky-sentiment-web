🚀 Bluesky Sentiment Web Dashboard has been deployed successfully!

📊 Service Information:
  Name: {{ include "bsky-sentiment-web.fullname" . }}
  Namespace: {{ .Release.Namespace }}
  Image: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}

{{- if .Values.httproute.enabled }}
🌐 Web Access:
  URL: https://{{ .Values.httproute.hostname }}
  
  Your dashboard is accessible at the URL above once DNS propagates.
{{- end }}

🔍 Service Access:
{{- if contains "ClusterIP" .Values.service.type }}
  # Get pod name
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "bsky-sentiment-web.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  
  # Port forward to access locally
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 3000:3000
  
  # Then access the dashboard at:
  🌐 Dashboard: http://localhost:3000

{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "bsky-sentiment-web.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  
  🌐 Dashboard: http://$NODE_IP:$NODE_PORT

{{- else if contains "LoadBalancer" .Values.service.type }}
     NOTE: It may take a few minutes for the LoadBalancer IP to be available.
           You can watch its status by running 'kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "bsky-sentiment-web.fullname" . }}'
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "bsky-sentiment-web.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  
  🌐 Dashboard: http://$SERVICE_IP:{{ .Values.service.port }}
{{- end }}

📝 Useful Commands:
  # View logs
  kubectl logs --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "bsky-sentiment-web.name" . }}" -f

  # Get pod status
  kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "bsky-sentiment-web.name" . }}"

  # Check service status
  kubectl get svc --namespace {{ .Release.Namespace }} {{ include "bsky-sentiment-web.fullname" . }}

{{- if .Values.httproute.enabled }}
  # Check HTTPRoute status
  kubectl get httproute --namespace {{ .Release.Namespace }} {{ include "bsky-sentiment-web.fullname" . }}
{{- end }}

  # View environment variables
  kubectl get secret --namespace {{ .Release.Namespace }} {{ (index .Values.envFrom 0).secretRef.name | default "bsky-sentiment-web-env" }} -o yaml

🔧 Configuration:
  Secret Name: bsky-sentiment-web-env
  
📚 Documentation: https://github.com/richardr1126/bsky-sentiment-web

Enjoy your real-time sentiment analysis dashboard! 📈✨
